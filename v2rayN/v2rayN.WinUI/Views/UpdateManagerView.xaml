<UserControl
    x:Class="v2rayN.WinUI.Views.UpdateManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <CommandBar DefaultLabelPosition="Right" IsOpen="True" IsDynamicOverflowEnabled="False">
            <AppBarToggleButton x:Name="PrereleaseButton" Label="检查预发布版本" Icon="Preview" />
            <AppBarButton Label="仅检查" Icon="Refresh" Command="{Binding CheckOnlyCmd}" />
            <AppBarButton Label="检查并更新" Icon="Download" Command="{Binding CheckUpdateCmd}" />
        </CommandBar>
        <ListView Grid.Row="1" ItemsSource="{Binding CheckUpdateModels}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid MinHeight="54" Padding="12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="44" /><ColumnDefinition Width="160" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding CoreTypeForStorage}" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="2" Text="{Binding Remarks}" TextWrapping="Wrap" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
